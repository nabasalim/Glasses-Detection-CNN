Glasses Detection using Deep Learning

This project implements a binary image classification system to detect
whether a person is wearing glasses or not using a custom CNN and
ResNet-50 transfer learning model.
